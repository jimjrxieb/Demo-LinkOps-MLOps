apiVersion: v1
kind: Service
metadata:
  name: {{ include "whis_sanitize.fullname" . }}
  labels:
    {{- include "whis_sanitize.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whis_sanitize.selectorLabels" . | nindent 4 }}
