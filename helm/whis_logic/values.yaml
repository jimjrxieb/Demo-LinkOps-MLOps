# Default values for whis_logic
replicaCount: 1

image:
  repository: linkops/whis_logic
  tag: "latest"
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 8003

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 200m
    memory: 256Mi

env:
  - name: LOG_LEVEL
    value: "INFO"
  - name: SERVICE_PORT
    value: "8003"
  - name: GROK_API_KEY
    valueFrom:
      secretKeyRef:
        name: grok-api-secret
        key: api-key

nodeSelector: {}

tolerations: []

affinity: {}
