apiVersion: v1
kind: Service
metadata:
  name: {{ include "whis_logic.fullname" . }}
  labels:
    {{- include "whis_logic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whis_logic.selectorLabels" . | nindent 4 }}
