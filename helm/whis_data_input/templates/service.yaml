apiVersion: v1
kind: Service
metadata:
  name: {{ include "whis_data_input.fullname" . }}
  labels:
    {{- include "whis_data_input.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whis_data_input.selectorLabels" . | nindent 4 }}
