{
  "name": "Safe Cleanup Rune",
  "description": "Safely clean up test resources and temporary files",
  "commands": [
    "kubectl delete namespace test-deployment --ignore-not-found=true",
    "docker system prune -f",
    "kubectl get pods --all-namespaces | grep -E '(Error|CrashLoopBackOff)' || echo 'No problematic pods found'",
    "find /tmp -name '*.tmp' -mtime +7 -delete",
    "journalctl --vacuum-time=7d"
  ],
  "validation": {
    "allowed_commands": ["kubectl", "docker", "find", "journalctl"],
    "denied_commands": ["rm -rf /", "shutdown", "dd", "mkfs"],
    "timeout_seconds": 120,
    "stop_on_failure": false
  }
} 